name: kiwoom
description: 키움증권 REST API Dart 클라이언트 — openapi.json에서 자동 생성됩니다.
version: 0.1.0
homepage: https://github.com/KKimj/Kiwoom-REST-API

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
